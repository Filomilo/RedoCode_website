name: generate documantation
on:
  push

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write  
    steps:
      - name: install javadoc
        run: |
          apt update
          apt install openjdk-21-jdk