version: '3'
networks:
  frontend:
    driver: bridge

services:
  frontend:
    build: ./RedoCode_frontend
    ports:
      - "5173:5173"
    volumes:
      - ./RedoCode_frontend/src:/frontend/src:delegated
    deploy:
      resources:
        limits:
          cpus: '0.1'
          memory: 1G
  backend:
    build: ./RedoCode_backend
    ports:
      - "9090:9090"
    volumes:
      - ./RedoCode_backend/src:/backend/src:delegated
    deploy:
      resources:
        limits:
          cpus: '0.1'
          memory: 1G
  db:
    image: postgres
    restart: always
    ports:
      - "5432:5432"  
    environment:
      POSTGRES_PASSWORD: password
    deploy:
      resources:
        limits:
          cpus: '0.1'
          memory: 1G
